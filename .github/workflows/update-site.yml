name: Update Lunch Deals Site

# on:
#   push:
#     branches: [ main ]
#   schedule:
#     - cron: '0 10 * * 1-5'  # Run at 10:00 AM on weekdays

# jobs:
#   update-deals:
#     runs-on: ubuntu-latest
#     steps:
#     - uses: actions/checkout@v2
    
#     - name: Set up Python
#       uses: actions/setup-python@v2
#       with:
#         python-version: '3.x'
    
#     - name: Install dependencies
#       run: |
#         python -m pip install --upgrade pip
#         pip install -r requirements.txt
    
#     - name: Run lunch scraper
#       run: python lunch_deal_manager.py
#       env:
#         OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
    
#     - name: Commit and push if changed
#       run: |
#         git config --local user.email "action@github.com"
#         git config --local user.name "GitHub Action"
#         git add lunch_data.json
#         git diff --quiet && git diff --staged --quiet || (git commit -m "Update lunch deals" && git push) 